Instructions for Use:
    1. Make sure you have a proper google or openai key and be sure to set it as an environment variable called 'GOOGLE_API_KEY' or 'OPENAI_API_KEY'.
    2. After setting your environment, be sure to edit config for use-case. The program can only use gemini calls at the moment.